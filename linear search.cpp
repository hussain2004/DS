#include<stdio.h>
int main()
{
	int flag,key,i,n,pos,a[i];
	for(i=0;i<n;i++)
	{
		if(a[i]=key)
		{
			flag=1;
			pos=i;
			break;
		}
	}
	if(flag=1)
	printf("element formal:%d",pos);
	else
	printf("elment not formal:%d");
	
}
